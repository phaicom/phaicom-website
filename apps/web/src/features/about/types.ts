export type Education = {
  period: string;
  title: string;
  institution: string;
  description: string;
};

export type Experience = {
  period: string;
  title: string;
  company: string;
  description: string;
};
