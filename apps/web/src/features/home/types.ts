import type { IconComponent } from "@/shared/types";

export type Service = {
  icon: IconComponent;
  title: string;
  description: string;
};

export type Skill = {
  name: string;
  icon: IconComponent;
};
